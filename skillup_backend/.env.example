# Optional — enables OpenAI for skill generation, coach, and improve.
# Without this key the API uses built-in fallbacks.
OPENAI_API_KEY=your-key-here

# Production (Render sets these via render.yaml / dashboard)
# DJANGO_SECRET_KEY=
# DJANGO_DEBUG=false
# DJANGO_ALLOWED_HOSTS=*
# DATABASE_URL=
# CORS_ALLOWED_ORIGINS=https://your-expo-web-host.example
